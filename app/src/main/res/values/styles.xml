<resources>
    <!-- Base application theme. -->
    <style name="AppBlankTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->

        <item name="android:textColor">@color/white</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/btn_add</item>
    </style>
    <style name="AppTheme" parent="AppBlankTheme">
        <!-- Customize your theme here. -->
        <!-- Base application thème. -->
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>

    </style>
    <style name="BtnAddWatchList">
        <item name="android:enabled">false</item>
        <item name="android:background">@drawable/border_radius</item>

        <item name="android:drawableStart">@color/white</item>

    </style>
    <style name="TextSecondary">
        <item name="android:textColor">@color/white_nuance</item>
    </style>
</resources>